name: daily aggregation (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Explain pause
        run: |
          echo "The legacy Transit Friction pipeline is paused."
          echo "Its event identity, lifecycle persistence, and aggregate metrics were not methodologically valid."
          echo "See README.md and the legacy-v0 branch before reactivating collection."
