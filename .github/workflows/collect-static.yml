name: static collector (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Explain pause
        run: |
          echo "The legacy Transit Friction pipeline is paused."
          echo "Static source work will resume only after the accessibility reboot passes its publication gates."
          echo "See README.md and the legacy-v0 branch."
